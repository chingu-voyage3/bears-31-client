// @flow
import * as React from 'react';

type MeetingsProps = {
    group_id: number,
    title: string,
    location: string,
    detail: string,
    due: number,
};

const MeetingList = () => {
  return (
    <div>
      Meeting Detail
    </div>
  );
};

export default MeetingList;
